
name: Run Add-On
on: repository_dispatch

jobs:
  Run-Add-On:
    uses: MuckRock/documentcloud-addon-workflows/.github/workflows/run-addon.yml@node-v2
    with:
      timeout: 30
      npm-packages: "filecoin-pin@2.1.1"
    secrets:
      token: ${{ secrets.PRIVATE_KEY }}
