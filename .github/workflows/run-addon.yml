
name: Run Add-On
on: repository_dispatch

jobs:
  Run-Add-On:
    uses: MuckRock/documentcloud-addon-workflows/.github/workflows/run-addon.yml@node
    with:
      timeout: 30
      npm-packages: "filecoin-pin@latest"
    secrets:
      token: ${{ secrets.PRIVATE_KEY }}
